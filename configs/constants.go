package configs

const (
	// InputPrefix prefix used to input files
	InputPrefix = "input-"
	// OutputPrefix prefix used to output files
	OutputPrefix = "output-"
	// FilePostfix postfix used to input and output files
	FilePostfix = ".txt"
)
